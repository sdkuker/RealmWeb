define([],
    function () {

        var utilities = {
            randomNumber : function(max) {
                return Math.floor(Math.random()*max);
            }
        };

        return utilities;

    });